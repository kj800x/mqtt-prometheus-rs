apiVersion: v1
kind: Service
metadata:
  name: mqtt-prometheus-rs
  namespace: home-sensors
spec:
  type: LoadBalancer
  selector:
    app: mqtt-prometheus-rs
  ports:
    - name: http
      protocol: TCP
      port: 9100
      targetPort: 9090
